name: Herve CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  echo-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: Build echo example
        working-directory: examples/echo
        run: make

      - name: Start echo server
        working-directory: examples/echo
        run: |
          make run &
          echo $! > server.pid
          sleep 2

      - name: Run echo tests
        run: |
          chmod +x scripts/test_echo.sh
          ./scripts/test_echo.sh

      - name: Stop echo server
        run: kill $(cat examples/echo/server.pid)

  malloc-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: Build malloc tests
        working-directory: lib/malloc/tests
        run: make

  json-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: Build JSON encoding tests
        working-directory: include/encoding/json/tests
        run: make

  html-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: Build HTML encoding tests
        working-directory: include/encoding/html/tests
        run: make

  array-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: Build array tests
        working-directory: lib/utils/tests
        run: make

